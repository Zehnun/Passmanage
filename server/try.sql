CREATE TABLE manage.muser ( Email varchar(255), Pass  varchar(255), ManId varchar(255) );

CREATE TABLE manage.box (
    ManId varchar(255),
    SelfId varchar(255),
    Names varchar(255),
    Email varchar(255),
    SePass varchar(255)
);

email pass ManId --> ManId selfId names email sepass
@gm.  x     3xdf      3xdf   1     fdf   s@f   fsd
                      3xdf   2     dff   dfd   dfd                   

implementation:

add:

add:


set up database
do operation CRUD back->database
connect front-back
display results
redign css, add edge cases
deploy